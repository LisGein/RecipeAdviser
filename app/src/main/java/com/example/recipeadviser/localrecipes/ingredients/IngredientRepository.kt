package com.example.recipeadviser.localrecipes.ingredients
/*
import androidx.lifecycle.LiveData
import com.example.recipeadviser.localrecipes.DataDao

class IngredientRepository(private val dataDao: DataDao) {

    val allIngredients: LiveData<List<IngredientData>> = dataDao.get_sorted_ingredients()

    suspend fun insert(Data: IngredientData) {
        dataDao.insert_ingredient(Data)
    }

    suspend fun remove(recipe_id: String) {
        dataDao.remove_ingredient(recipe_id)
    }

}*/