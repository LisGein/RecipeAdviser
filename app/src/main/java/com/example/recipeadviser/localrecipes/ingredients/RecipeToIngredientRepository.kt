package com.example.recipeadviser.localrecipes.ingredients
/*
import androidx.lifecycle.LiveData
import com.example.recipeadviser.localrecipes.DataDao

class RecipeToIngredientRepository(private val dataDao: DataDao) {

    val allData: LiveData<List<RecipeToIngredientData>> = dataDao.get_sorted_recipe_to_ingredients()

    suspend fun insert(Data: RecipeToIngredientData) {
        dataDao.insert_recipe_to_ingredients(Data)
    }

    suspend fun remove(id: String) {
        dataDao.remove_recipe_to_ingredients(id)
    }

}*/